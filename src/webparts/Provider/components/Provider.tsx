import * as React from 'react'
import { IProviderProps } from './IProviderProps'

const Provider = ({ }: IProviderProps): JSX.Element => {
  return (
    <div>Provider</div>
  )
}

export default Provider